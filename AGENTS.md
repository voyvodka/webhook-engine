# webhook-engine — Agent Instructions

Self-hosted webhook delivery platform: .NET 10 API, background workers and a React dashboard in one host, with PostgreSQL as both store and queue.

**Read `CLAUDE.md` first.** It is the main context source for this repository: stack, rules and conventions, and where the documentation lives. This file exists only so that tools looking for `AGENTS.md` land in the right place.

Do not duplicate project context into this file. One source, one place to keep current.
